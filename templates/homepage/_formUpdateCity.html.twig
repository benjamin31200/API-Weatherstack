{{ form_start(form) }}
<div class="formCity">
	{{ form_row(form.city, {
		label: 'Ville actuelle:'
	}) }}
	<button class="btn">{{ button_label|default('Save') }}</button>
</div>

{{ form_end(form) }}
